# PAX Detect - person detection using OpenCV
Person detection using OpenCV HOG person detection.

## Running
Build the docker image and run detection.sh

```bash
docker build -t py3:opencv .
./detection.sh
```

